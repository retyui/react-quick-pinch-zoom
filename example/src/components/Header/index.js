import React from "react";

import "./styles.css";

const Header = () => (
  <header className="c-header">
    <h1 className="c-header-title">react-quick-pinch-zoom</h1>
    <p>
      A react component that lets you zooming and dragging on any DOM element
      using multi-touch gestures on mobile devices and mouse-events\wheel on
      desktop devices.
      <a href="https://github.com/retyui/react-quick-pinch-zoom">
        Open on github...
      </a>
    </p>
  </header>
);

export default Header;
